# No secrets are required for the public American Soccer Analysis endpoints.
# Optional overrides:

# KEEPERIQ_LOG_LEVEL=INFO
# MLS_KEEPERIQ_FORCE_REFRESH is not used; pass --force to the CLI instead.
